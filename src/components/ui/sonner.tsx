import React from 'react';
import { View } from 'react-native';

// No-op RN toaster placeholder. Replace with a native toast library if needed.
const Toaster = (_props: any) => {
  return <View />;
};

export { Toaster };
