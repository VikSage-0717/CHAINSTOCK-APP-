// Configuration for optional external services.
// To enable real news fetching, set `NEWS_API_KEY` to your NewsAPI.org API key.
// NOTE: Embedding API keys in client-side apps exposes them publicly. For production,
// proxy requests through a backend service.

export const NEWS_API_KEY = '';
